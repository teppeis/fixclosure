/**
 * @interface
 * @extends {goog.disposable.IDisposable}
 */
var Foo = function() {
};

/**
 * @constructor
 * @extends {goog.Disposable}
 */
var Foo = function() {
};

// toRequire: goog.disposable.IDisposable
